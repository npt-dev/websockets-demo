const connect = require('./connect');
const disconnect = require('./disconnect');
const isDefault = require('./default');
const onMessage = require('./onMessage');

module.exports = {
    connect,
    disconnect,
    isDefault,
    onMessage
}